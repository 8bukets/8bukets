# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Added by GitGuard (Node.js, Next.js, React, Python)
# Environment files contain secrets, API keys, and database credentials that should never be committed
.env
# Local environment overrides often contain developer-specific secrets
.env.local
# Environment-specific local files may contain sensitive overrides
.env.*.local
# Development environment secrets should not be shared
.env.development.local
# Production secrets must never be committed to version control
.env.production.local
node_modules/
dist/
build/
out/
coverage/
.next/
# Test environment secrets should not be committed
.env.test.local
# Any environment file may contain sensitive configuration
*.env
venv/
.venv/
env/
ENV/
__pycache__/
*.py[cod]
*.so
.ipynb_checkpoints/
# SQLite database files may contain sensitive data
*.sqlite3
# Django default SQLite database should not be committed
db.sqlite3
*.db
# Secrets file should never be committed
secrets.json
# Local settings often contain secrets and should not be committed
local_settings.py

# Antigravity Runtime Artifacts
.jules_memory.json
.antigravity/.jules_memory.json
autonomous_state.json
CONSOLIDATED_INTELLIGENCE.md
KNOWLEDGE_MERGE.md
system_knowledge.json
data/work_orders.json
api.log
dashboard.log
dashboard.error.log
jules_daily.log
jules_daily.error.log
orchestrator.log
orchestrator.error.log
syra_api.log
syra_api.error.log
syra_api_enterprise.log
daily_output.txt
