## 2026-02-02 - Raw Data Structures in User Reports
**Learning:** Exposing raw Python list representations (e.g., `['a', 'b']`) in user-facing Markdown reports degrades readability and looks unprofessional.
**Action:** Always iterate through collections and format them as Markdown lists (bullets) or comma-separated strings for cleaner presentation.
