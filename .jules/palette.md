## 2024-05-23 - [CLI Visual Polish]
**Learning:** Adding colored summary boxes to CLI tools significantly improves the perception of completion and success, especially for long-running tasks. Users often miss standard log lines.
**Action:** Implement `Colors` class and `print_summary` function in CLI scripts to highlight key metrics.
