## 2026-02-05 - ASCII Visualization in Markdown Reports
**Learning:** Adding simple ASCII bar charts (e.g., `████░░░`) to markdown tables significantly improves the scannability of data distributions in CLI-generated reports without adding external dependencies.
**Action:** Use `create_ascii_bar` helper pattern for future CLI report enhancements.
