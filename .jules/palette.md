## 2026-01-02 - [Markdown Report Readability]
**Learning:** Large tables in Markdown reports can overwhelm the user, making it hard to find other critical information like "Recommendations" or "Trends".
**Action:** Use HTML `<details>` and `<summary>` tags to collapse large datasets (like "Recently Scraped Posts") by default, improving the report's scanability.
