## 2025-12-21 - CLI Logging UX
**Learning:** Standard Python logging can be easily enhanced with a custom `Formatter` to add visual hierarchy (colors, emojis) to CLI tools without external dependencies, making logs much easier to scan.
**Action:** Use `UXFormatter` pattern in other CLI scripts to standardize output.
