## 2024-05-22 - Performance Learning
**Learning:** BeautifulSoup parsing of large documents can be significantly optimized using `SoupStrainer` to limit parsing to specific tags.
**Action:** Use `SoupStrainer` when only a subset of the DOM is needed.
