name: Test system_health_dashboard

on:
  push:
    paths:
      - 'antigravity/services/system_health_dashboard.ts'
      - 'antigravity/services/system_health_dashboard.test.ts'
  pull_request:
    paths:
      - 'antigravity/services/system_health_dashboard.ts'
      - 'antigravity/services/system_health_dashboard.test.ts'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
      - name: Install Dependencies
        run: npm ci
      - name: Run Tests
        run: npx vitest run antigravity/services/system_health_dashboard.test.ts
