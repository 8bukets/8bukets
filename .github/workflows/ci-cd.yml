name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Log in to the Container registry (GHCR)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Docker Image
        run: make docker-build
        env:
          IMAGE_NAME: getanant/sor
          TAG: latest

      - name: Push to Docker Hub
        run: make docker-push
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
          IMAGE_NAME: getanant/sor
          TAG: latest

      - name: Push to GHCR
        run: |
          docker tag getanant/sor:latest ghcr.io/${{ github.repository }}/sor:latest
          docker push ghcr.io/${{ github.repository }}/sor:latest

      # Previous GCP deployment steps retained but commented out for potential future use
      # - name: Authenticate to Google Cloud
      #   uses: google-github-actions/auth@v2
      #   with:
      #     credentials_json: ${{ secrets.GCP_CREDENTIALS }}
      #
      # - name: Set up Cloud SDK
      #   uses: google-github-actions/setup-gcloud@v2
      #
      # - name: Deploy to Cloud Run
      #   run: |
      #     gcloud run deploy sor-app \
      #       --image ghcr.io/${{ github.repository }}/sor:latest \
      #       --platform managed \
      #       --region us-central1 \
      #       --allow-unauthenticated
