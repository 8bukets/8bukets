name: Test ai_strategy_advisor

on:
  push:
    paths:
      - 'antigravity/services/ai_strategy_advisor.ts'
      - 'antigravity/services/ai_strategy_advisor.test.ts'
  pull_request:
    paths:
      - 'antigravity/services/ai_strategy_advisor.ts'
      - 'antigravity/services/ai_strategy_advisor.test.ts'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
      - name: Install Dependencies
        run: npm ci
      - name: Run Tests
        run: npx vitest run antigravity/services/ai_strategy_advisor.test.ts
