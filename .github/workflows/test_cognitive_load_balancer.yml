name: Test cognitive_load_balancer

on:
  push:
    paths:
      - 'antigravity/services/cognitive_load_balancer.ts'
      - 'antigravity/services/cognitive_load_balancer.test.ts'
  pull_request:
    paths:
      - 'antigravity/services/cognitive_load_balancer.ts'
      - 'antigravity/services/cognitive_load_balancer.test.ts'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Install Dependencies
        run: npm ci
      - name: Run Tests
        run: npx vitest run antigravity/services/cognitive_load_balancer.test.ts
