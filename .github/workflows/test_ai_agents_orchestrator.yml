name: Test ai_agents_orchestrator

on:
  push:
    paths:
      - 'antigravity/services/ai_agents_orchestrator.ts'
      - 'antigravity/services/ai_agents_orchestrator.test.ts'
  pull_request:
    paths:
      - 'antigravity/services/ai_agents_orchestrator.ts'
      - 'antigravity/services/ai_agents_orchestrator.test.ts'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Install Dependencies
        run: npm ci
      - name: Run Tests
        run: npx vitest run antigravity/services/ai_agents_orchestrator.test.ts
