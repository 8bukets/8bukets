name: Test performance_scalability_bridge

on:
  push:
    paths:
      - 'antigravity/services/performance_scalability_bridge.ts'
      - 'antigravity/services/performance_scalability_bridge.test.ts'
  pull_request:
    paths:
      - 'antigravity/services/performance_scalability_bridge.ts'
      - 'antigravity/services/performance_scalability_bridge.test.ts'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Install Dependencies
        run: npm ci
      - name: Run Tests
        run: npx vitest run antigravity/services/performance_scalability_bridge.test.ts
