name: Test global_neural_sync_service_phase_12

on:
  push:
    paths:
      - 'antigravity/services/global_neural_sync_service_phase_12.ts'
      - 'antigravity/services/global_neural_sync_service_phase_12.test.ts'
  pull_request:
    paths:
      - 'antigravity/services/global_neural_sync_service_phase_12.ts'
      - 'antigravity/services/global_neural_sync_service_phase_12.test.ts'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Install Dependencies
        run: npm ci
      - name: Run Tests
        run: npx vitest run antigravity/services/global_neural_sync_service_phase_12.test.ts
