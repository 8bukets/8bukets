name = "sor8bukets"
main = "src/index.js"
compatibility_date = "2026-06-24"

# Restored routes, disabling custom_domain for sandbox/preview build stability
routes = [
  { pattern = "sor8bukets.example.com", custom_domain = false }
]
