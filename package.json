{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "build": "ncc build index.js -o dist && ncc build pre.js -o dist/pre && ncc build post.js -o dist/post"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^5.1.1",
    "tsx": "^4.21.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  }
}
