/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import type { OAuthClientProvider } from '@modelcontextprotocol/sdk/client/auth.js';
import type { OAuthClientInformation, OAuthClientMetadata, OAuthTokens } from '@modelcontextprotocol/sdk/shared/auth.js';
import type { MCPServerConfig } from '../config/config.js';
export declare class DynamicStoredOAuthProvider implements OAuthClientProvider {
    private readonly serverName;
    private readonly serverConfig;
    readonly redirectUrl = "";
    readonly clientMetadata: OAuthClientMetadata;
    private clientInfo?;
    private readonly oauthProvider;
    private cachedToken?;
    private tokenExpiryTime?;
    constructor(serverName: string, serverConfig: MCPServerConfig);
    clientInformation(): OAuthClientInformation | undefined;
    saveClientInformation(clientInformation: OAuthClientInformation): void;
    private isCachedTokenValid;
    tokens(): Promise<OAuthTokens | undefined>;
    saveTokens(_tokens: OAuthTokens): void;
    redirectToAuthorization(_authorizationUrl: URL): void;
    saveCodeVerifier(_codeVerifier: string): void;
    codeVerifier(): string;
}
