/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { MCPOAuthProvider } from './oauth-provider.js';
import { FIVE_MIN_BUFFER_MS } from './oauth-utils.js';
export class DynamicStoredOAuthProvider {
    serverName;
    serverConfig;
    redirectUrl = '';
    clientMetadata = {
        client_name: 'Gemini CLI (Stored OAuth)',
        redirect_uris: [],
        grant_types: [],
        response_types: [],
        token_endpoint_auth_method: 'none',
    };
    clientInfo;
    oauthProvider = new MCPOAuthProvider();
    cachedToken;
    tokenExpiryTime;
    constructor(serverName, serverConfig) {
        this.serverName = serverName;
        this.serverConfig = serverConfig;
    }
    clientInformation() {
        return this.clientInfo;
    }
    saveClientInformation(clientInformation) {
        this.clientInfo = clientInformation;
    }
    isCachedTokenValid() {
        return !!(this.cachedToken?.access_token &&
            this.tokenExpiryTime &&
            Date.now() < this.tokenExpiryTime - FIVE_MIN_BUFFER_MS);
    }
    async tokens() {
        if (this.isCachedTokenValid()) {
            return this.cachedToken;
        }
        const oauthConfig = this.serverConfig.oauth?.enabled && this.serverConfig.oauth
            ? this.serverConfig.oauth
            : {};
        const tokenMeta = await this.oauthProvider.getValidTokenWithMetadata(this.serverName, oauthConfig);
        if (!tokenMeta?.accessToken) {
            this.cachedToken = undefined;
            this.tokenExpiryTime = undefined;
            return undefined;
        }
        const freshTokens = {
            access_token: tokenMeta.accessToken,
            token_type: tokenMeta.tokenType || 'Bearer',
            expires_in: tokenMeta.expiresAt
                ? Math.max(0, Math.floor((tokenMeta.expiresAt - Date.now()) / 1000))
                : undefined,
            scope: tokenMeta.scope,
            refresh_token: tokenMeta.refreshToken,
        };
        if (freshTokens.expires_in !== undefined) {
            this.cachedToken = freshTokens;
            this.tokenExpiryTime = Date.now() + freshTokens.expires_in * 1000;
            return this.cachedToken;
        }
        this.cachedToken = undefined;
        this.tokenExpiryTime = undefined;
        return freshTokens;
    }
    saveTokens(_tokens) { }
    redirectToAuthorization(_authorizationUrl) { }
    saveCodeVerifier(_codeVerifier) { }
    codeVerifier() {
        return '';
    }
}
//# sourceMappingURL=stored-token-provider.js.map