/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { GenerateContentResponse, type CountTokensResponse, type GenerateContentParameters, type CountTokensParameters, EmbedContentResponse, type EmbedContentParameters } from '@google/genai';
import type { ContentGenerator } from './contentGenerator.js';
import type { UserTierId, GeminiUserTier } from '../code_assist/types.js';
import type { LlmRole } from '../telemetry/types.js';
export type FakeResponse = {
    method: 'generateContent';
    response: GenerateContentResponse;
} | {
    method: 'generateContentStream';
    response: GenerateContentResponse[];
} | {
    method: 'countTokens';
    response: CountTokensResponse;
} | {
    method: 'embedContent';
    response: EmbedContentResponse;
};
/**
 * Options for the FakeContentGenerator.
 */
export interface FakeContentGeneratorOptions {
    /**
     * If true, the generator will find the first available response that matches
     * the requested method, rather than strictly following the input order.
     * Useful for non-deterministic background tasks.
     */
    nonStrict?: boolean;
}
export declare class FakeContentGenerator implements ContentGenerator {
    private readonly options;
    private callCounter;
    userTier?: UserTierId;
    userTierName?: string;
    paidTier?: GeminiUserTier;
    private readonly responses;
    constructor(responses: FakeResponse[], options?: FakeContentGeneratorOptions);
    static fromFile(filePath: string, options?: FakeContentGeneratorOptions): Promise<FakeContentGenerator>;
    private getNextResponse;
    generateContent(request: GenerateContentParameters, _userPromptId: string, role: LlmRole): Promise<GenerateContentResponse>;
    generateContentStream(request: GenerateContentParameters, _userPromptId: string, role: LlmRole): Promise<AsyncGenerator<GenerateContentResponse>>;
    countTokens(request: CountTokensParameters): Promise<CountTokensResponse>;
    embedContent(request: EmbedContentParameters): Promise<EmbedContentResponse>;
}
