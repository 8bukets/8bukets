/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import type { AgentLoopContext } from '../config/agent-loop-context.js';
import { AgentSession } from '../agent/agent-session.js';
import { type LocalAgentDefinition, type OutputObject, type SubagentActivityEvent } from './types.js';
import type { MessageBus } from '../confirmation-bus/message-bus.js';
export declare class LocalSubagentSession extends AgentSession {
    private readonly _localProtocol;
    constructor(definition: LocalAgentDefinition, context: AgentLoopContext, messageBus: MessageBus, rawActivityCallback?: (activity: SubagentActivityEvent) => void);
    /**
     * Returns the raw executor OutputObject once execution completes.
     * Used by LocalSubagentInvocation to build the ToolResult.
     */
    getResult(): Promise<OutputObject>;
}
