/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import type { ConcreteNode } from '../graph/types.js';
import type { ContextEnvironment } from '../pipeline/environment.js';
export interface SnapshotState {
    active_tasks: Array<{
        id: string;
        description: string;
    }>;
    discovered_facts: string[];
    constraints_and_preferences: string[];
    recent_arc: string[];
}
export declare function isSnapshotState(text: string): boolean;
export interface BaselineSnapshotInfo {
    text: string;
    abstractsIds: string[];
    id: string;
    timestamp: number;
}
/**
 * Global Lookback: Scans the target nodes in reverse to find the absolute
 * most recent valid Snapshot node to use as a Delta baseline.
 */
export declare function findLatestSnapshotBaseline(targets: readonly ConcreteNode[]): BaselineSnapshotInfo | undefined;
export declare class SnapshotGenerator {
    private readonly env;
    constructor(env: ContextEnvironment);
    synthesizeSnapshot(nodes: readonly ConcreteNode[], previousStateJson?: string, options?: {
        maxSummaryTurns?: number;
        maxStateTokens?: number;
    }): Promise<string>;
}
/**
 * Shared logic for working with Snapshot node state.
 */
export declare class SnapshotStateHelper {
    /**
     * Flatten nested abstract IDs to only the "pristine" (non-snapshot) IDs.
     */
    static flattenAbstracts(nodes: ConcreteNode[], abstractsIds: readonly string[]): string[];
    /**
     * Helper to extract state from the most recent snapshot in a list of nodes.
     */
    static exportState(nodes: ConcreteNode[]): {
        snapshot?: {
            text: string;
            consumedIds: string[];
        };
    };
}
