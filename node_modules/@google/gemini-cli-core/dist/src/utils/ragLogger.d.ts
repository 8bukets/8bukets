/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
export interface RagSnippet {
    repository?: string;
    filePath?: string;
    startLine?: number;
    endLine?: number;
    relevanceScore?: number;
    content: string;
}
export interface RagLogEntry {
    timestamp: string;
    sessionId: string;
    ragStatus: string;
    snippets: RagSnippet[];
}
export declare class RagLogger {
    private logPath;
    private hasInitializedFile;
    /**
     * Initializes the logger with the project's temporary logs directory.
     */
    initialize(logsDir: string): void;
    /**
     * Logs a RAG trace entry as JSONL.
     */
    log(entry: Omit<RagLogEntry, 'timestamp'>): void;
}
export declare const ragLogger: RagLogger;
