/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { type Config } from '../config/config.js';
export interface ResolvedAtCommandPath {
    absolutePath: string;
    relativePath: string;
    stats: {
        isDirectory(): boolean;
        isFile(): boolean;
    };
}
/**
 * Result of a path resolution attempt.
 */
export type ResolveAtCommandPathResult = {
    status: 'resolved';
    resolved: ResolvedAtCommandPath;
} | {
    status: 'unauthorized';
    absolutePath: string;
    error: string;
} | {
    status: 'invalid';
    error: string;
} | {
    status: 'not_found';
};
/**
 * Resolves a path from an @-command, ensuring it is valid and within workspace boundaries.
 * Performs best-effort extraction if the input appears to be a misinterpreted log fragment.
 */
export declare function resolveAtCommandPath(pathName: string, config: Config, onDebugMessage?: (msg: string) => void): Promise<ResolveAtCommandPathResult>;
