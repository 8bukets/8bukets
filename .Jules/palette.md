## 2026-01-14 - Visual Density in Text Reports
**Learning:** Adding simple ASCII bar charts (e.g., `████`) to Markdown tables significantly improves data scanning without adding external dependencies.
**Action:** Look for other tabular outputs (CLI logs, generated summaries) that could benefit from "visual density" columns.
