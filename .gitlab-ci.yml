build-job:
  stage: build
  script:
    - echo "Hello, $GITLAB_USER_LOGIN!"

test-job1:
  stage: test
  script:
    - echo "This job tests something"

test-job2:
  stage: test
  script:
    - echo "This job tests something, but takes more time than test-job1."
    - echo "After the echo commands complete, it runs the sleep command for 20 seconds"
    - echo "which simulates a test that runs 20 seconds longer than test-job1"
    - sleep 20

deploy-prod:
  stage: deploy
  script:
    - echo "This job deploys something from the $CI_COMMIT_BRANCH branch."
  environment: production

run-autonomous-feedback_analysis:
  stage: test
  script:
    - echo "Running autonomous cycle for Feedback Analysis Service"
    - npx tsx antigravity/workflows/feedback_analysis_workflow.ts
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"

run-autonomous-performance_monitoring:
  stage: test
  script:
    - echo "Running autonomous cycle for Performance Monitoring Service"
    - npx tsx antigravity/workflows/performance_monitoring_workflow.ts
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"

run-autonomous-autonomous_discovery_engine:
  stage: test
  script:
    - echo "Running autonomous cycle for Autonomous Discovery Engine"
    - npx tsx antigravity/workflows/autonomous_discovery_engine_workflow.ts
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"

run-autonomous-edge-to-cloud_bridge:
  stage: test
  script:
    - echo "Running autonomous cycle for Edge-to-Cloud Bridge"
    - npx tsx antigravity/workflows/edge-to-cloud_bridge_workflow.ts
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"

run-autonomous-edge_to_cloud_bridge:
  stage: test
  script:
    - echo "Running autonomous cycle for Edge-to-Cloud Bridge"
    - npx tsx antigravity/workflows/edge_to_cloud_bridge_workflow.ts
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"
